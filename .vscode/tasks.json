  {
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build: Debug (Test-Build)",
            "type": "shell",
            "command": "avr-gcc -mmcu=atmega328p -DF_CPU=16000000UL -O0 -g -DDEBUG main.c -o main.elf && avr-objcopy -O ihex main.elf main.hex",
            "group": "build",
            "problemMatcher": ["$gcc"]
        },
        {
            "label": "Build: Release (Production)",
            "type": "shell",
            "command": "avr-gcc -mmcu=atmega328p -DF_CPU=16000000UL -Os main.c -o main.elf && avr-objcopy -O ihex main.elf main.hex",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}